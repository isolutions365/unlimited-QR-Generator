# Netlify Redirect Rules
# Serve static resources from the root without rewriting
/sitemap.xml  /sitemap.xml  200
/ads.txt      /ads.txt      200

# Support Single Page Application routing (rewrites all paths to index.html with a 200)
/*            /index.html   200
